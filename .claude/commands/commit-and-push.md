Commit all changes and push to GitHub using the correct account (timothywangdev).

Steps:
1. Stage all changes: `git add -A`
2. Create a commit with a descriptive message based on the changes
3. Switch GitHub account: `gh auth switch --user timothywangdev`
4. Push: `git push`
5. Switch back to main account: `gh auth switch --user timothy-web3-dev`

If any step fails, still switch back to the main account before reporting the error.
